[package]
name = "region_cluster_processor"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
map-partition-sdk = { path = "../.." }
arrow = { version = "54" }
paste = "1"
